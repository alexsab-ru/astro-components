export type TStep = {
	id?: string
	title?: string,
  description?: string,
  type?: string,
  answerOptions?: string[],
  backgroundImageUrl?: string,
  backgroundPosition?: string,
  startButtonText?: string
}