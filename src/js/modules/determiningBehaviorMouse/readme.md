# Система защиты от ботов - Определение поведения мыши

Интеллектуальная система анализа поведения пользователя для защиты форм от спам-ботов.

## 📋 Описание

Эта система отслеживает поведение пользователя при работе с формами и определяет подозрительную активность на основе анализа:
- Движения мыши (траектория, скорость, изменения направления)
- Времени заполнения формы
- Паттернов взаимодействия с элементами

Система **не блокирует** отправку формы, а собирает метрики, которые затем отправляются на сервер для принятия решения.

## 🎯 Принцип работы

### 1. Отслеживание движения мыши
При загрузке страницы начинается непрерывное отслеживание событий `mousemove`. Каждое движение мыши сохраняется в виде точки с координатами (x, y) и временем события.

### 2. Отслеживание времени заполнения формы
Когда пользователь начинает взаимодействовать с формой (фокус на поле, ввод текста), запускается таймер для этой конкретной формы.

### 3. Сбор метрик при отправке
При нажатии кнопки отправки (событие `submit`) система:
1. Вычисляет все метрики поведения мыши
2. Вычисляет время заполнения формы
3. Выводит результаты в консоль
4. Готовит данные для отправки на сервер

## 📊 Метрики

### Метрики мыши (`mouseBehavior`)

#### 1. **directionChanges** - Количество изменений направления
- **Как вычисляется:** Подсчитывает количество значительных (>15°) изменений угла движения мыши
- **Боты:** < 15 (прямолинейное движение)
- **Люди:** > 30 (много мелких корректировок траектории)

#### 2. **smoothnessScore** - Коэффициент плавности траектории
- **Как вычисляется:** Отношение общего пройденного расстояния к прямому расстоянию между начальной и конечной точкой
- **Формула:** `totalDistance / straightDistance`
- **Боты:** 1.0-1.2 (движение почти по прямой)
- **Люди:** 1.5-3.0+ (извилистая траектория)

#### 3. **speedAndAcceleration** - Стандартное отклонение скорости
- **Как вычисляется:** Стандартное отклонение скорости движения между последовательными точками
- **Боты:** < 50 (постоянная скорость)
- **Люди:** > 100 (переменная скорость с ускорениями и замедлениями)

#### 4. **timeBetweenMouseEvents** - Вариация временных интервалов
- **Как вычисляется:** Стандартное отклонение интервалов времени между событиями `mousemove`
- **Боты:** < 5ms (идеально равномерные интервалы, часто ровно 16ms)
- **Люди:** > 10ms (естественная вариация)

#### 5. **mouseActivityBeforeSending** - Количество событий мыши
- **Как вычисляется:** Общее количество зафиксированных событий `mousemove`
- **Боты:** 0-10 событий (минимальная или отсутствующая активность)
- **Люди:** 50-100+ событий (активное движение по странице)

### Метрика времени заполнения

#### **formFillingTime** - Время заполнения формы
- **Как вычисляется:** Разница между временем первого взаимодействия с формой и временем отправки
- **Боты:** < 3000ms (заполнение менее чем за 3 секунды)
- **Люди:** > 5000ms (среднее время заполнения 5-30 секунд)

### Информация об устройстве (`device`)

Собирается один раз при загрузке страницы и включает в себя детальную информацию о браузере, устройстве и окружении пользователя.

#### 1. **Основная информация**
- `type` - Тип устройства (desktop, mobile, tablet)
- `os` - Операционная система с версией (например, "Windows 10", "macOS 14.0", "Android 13")
- `browser` - Объект с названием и версией браузера
- `userAgent` - Полная строка User Agent

#### 2. **Информация о экране** (`screen`)
- `screenWidth/screenHeight` - Полное разрешение экрана
- `availableWidth/availableHeight` - Доступная область (без панелей ОС)
- `windowWidth/windowHeight` - Размер окна браузера
- `pixelRatio` - Плотность пикселей (для retina дисплеев = 2+)
- `colorDepth` - Глубина цвета
- `orientation` - Ориентация экрана (landscape/portrait)

#### 3. **Язык и локализация** (`language`)
- `language` - Основной язык пользователя
- `languages` - Массив всех языков в порядке предпочтения
- `timezone` - Временная зона (например, "Europe/Moscow")
- `timezoneOffset` - Смещение от UTC в минутах

#### 4. **Сетевое подключение** (`connection`)
- `effectiveType` - Тип подключения (slow-2g, 2g, 3g, 4g)
- `downlink` - Примерная скорость в Мбит/с
- `rtt` - Задержка в миллисекундах
- `saveData` - Режим экономии трафика (true/false)

#### 5. **Информация о железе** (`hardware`)
- `cpuCores` - Количество логических процессоров
- `deviceMemory` - Объем памяти устройства в ГБ
- `maxTouchPoints` - Максимальное количество точек касания
- `webGLSupport` - Поддержка WebGL
- `gpuVendor` - Производитель графической карты

#### 6. **Обнаружение headless браузеров** (`headless`) 🤖
Специальная проверка для выявления автоматизированных браузеров (используются ботами):
- `isHeadless` - true если обнаружены подозрительные признаки
- `suspiciousFeatures` - Массив обнаруженных признаков:
  - `webdriver_flag` - флаг WebDriver включен
  - `no_plugins` - отсутствуют плагины браузера
  - `zero_outer_dimensions` - нулевые размеры окна
  - `dimension_mismatch` - несоответствие размеров
  - `missing_chrome_object` - отсутствует объект chrome в Chrome
  - `phantomjs_detected` - обнаружен PhantomJS
  - `automation_tools_detected` - обнаружены инструменты автоматизации

#### 7. **Дополнительная информация**
- `touchSupport` - Поддержка сенсорного экрана
- `platform` - Платформа (например, "MacIntel", "Win32")
- `vendor` - Производитель браузера
- `cookiesEnabled` - Включены ли cookies
- `doNotTrack` - Настройка "Не отслеживать"
- `timestamp` - Время сбора данных

## 🏗️ Архитектура

```
determiningBehaviorMouse/
├── determiningBehaviorMouse.js      # Главный модуль, координация
├── _calculateMouseTrajectory.js     # Сбор траектории мыши
├── _calculateDirectionChanges.js    # Вычисление изменений направления
├── _calculateSmoothnessScore.js     # Вычисление плавности траектории
├── _calculateSpeedAndAcceleration.js # Вычисление вариации скорости
├── _calculateTimeBetweenMouseEvents.js # Вычисление интервалов между событиями
├── _calculateMouseActivityBeforeSending.js # Подсчет активности мыши
├── _calculateFormFillingTime.js     # Отслеживание времени заполнения
├── _determiningDevice.js            # Определение устройства и окружения
└── readme.md                        # Эта документация
```

### Модульная структура
Каждая метрика вычисляется в отдельном файле для:
- ✅ Простоты поддержки и отладки
- ✅ Возможности тестирования отдельных компонентов
- ✅ Легкого добавления новых метрик

## 🔧 Использование

### Автоматическая инициализация
Система автоматически инициализируется при импорте модуля в `app.js`:

```javascript
import './modules/determiningBehaviorMouse/determiningBehaviorMouse.js';
```

### Получение данных вручную
Если нужно получить метрики в произвольный момент:

```javascript
import { calculateMouseMetrics, data } from './modules/determiningBehaviorMouse/determiningBehaviorMouse.js';

// Вычислить метрики
calculateMouseMetrics();

// Получить данные
console.log(data);
```

### Обработка отправки формы
Система автоматически перехватывает все события `submit` на странице. Данные о поведении выводятся в консоль при отправке любой формы.

## 📱 Поддержка мобильных устройств

**⚠️ ВАЖНО:** Текущая версия оптимизирована для десктопных устройств.

### Автоматическое определение типа устройства
Система автоматически определяет тип устройства через `data.device.type`:
- `desktop` - Стационарный компьютер или ноутбук
- `mobile` - Смартфон
- `tablet` - Планшет

### Адаптация проверок
На мобильных устройствах отсутствуют события мыши (используются touch-события: `touchstart`, `touchmove`, `touchend`), поэтому:
- **Метрики мыши** на мобильных будут близки к нулю (это нормально!)
- **Для мобильных** следует использовать только: `formFillingTime` и `device.headless`
- **Проверка типа устройства:**
```javascript
if (data.device.type === 'mobile' || data.device.touchSupport) {
  // Используем упрощенные критерии для мобильных
}
```

**Планируется:**
- Добавить отслеживание touch-событий
- Автоматически отключать проверки мыши для мобильных
- Использовать альтернативные метрики для touch-устройств

## 🎯 Критерии определения бота

### Для десктопных устройств
Поведение считается подозрительным, если **2 или более** метрик не соответствуют норме:

| Метрика | Норма для человека | Подозрительно (бот) |
|---------|-------------------|---------------------|
| directionChanges | > 15 | < 15 |
| smoothnessScore | > 1.2 | < 1.2 |
| speedAndAcceleration | > 50 | < 50 |
| timeBetweenMouseEvents | > 5 | < 5 |
| mouseActivityBeforeSending | > 10 | < 10 |
| formFillingTime | > 3000ms | < 3000ms |
| device.headless.isHeadless | false | true |

### Для мобильных устройств
Проверяются только:
- `formFillingTime` > 3000ms (не менее 3 секунд)
- `device.headless.isHeadless` = false (не headless браузер)
- Соответствие User Agent заявленному устройству

### Критические признаки бота
Эти признаки сами по себе очень подозрительны:
- ✅ `device.headless.isHeadless === true` - обнаружен headless браузер
- ✅ `formFillingTime < 1000ms` - заполнение менее чем за секунду
- ✅ `mouseActivityBeforeSending === 0` на desktop - нет движения мыши вообще
- ✅ Несоответствие данных (iOS + Android User Agent)

## 🔒 Стратегия защиты

### "Тихая блокировка"
Система **не блокирует** форму на клиенте. Вместо этого:

1. **Клиент:** Собирает метрики и отправляет их на сервер вместе с данными формы
2. **Сервер:** Анализирует метрики и принимает решение:
   - Если подозрительно → молча игнорирует заявку (но отвечает "success")
   - Если нормально → обрабатывает заявку

### Преимущества подхода:
- ✅ Бот не знает, что его вычислили
- ✅ Не раздражает настоящих пользователей
- ✅ Гибкая настройка критериев на сервере

## 🚀 Следующие шаги (TODO)

### 1. Добавить функцию проверки на бота
```javascript
function isSuspiciousBehavior() {
  let suspiciousCount = 0;
  
  if (data.mouseBehavior.directionChanges < Criteria.DIRECTION_CHANGES) suspiciousCount++;
  if (data.mouseBehavior.smoothnessScore < Criteria.SMOOTHNESS_SCORE) suspiciousCount++;
  // ... остальные проверки
  
  return suspiciousCount >= 2;
}
```

### 2. Добавить метрики в форму перед отправкой
```javascript
function addMetricsToForm(form) {
  const fields = {
    '_mouse_changes': data.mouseBehavior.directionChanges,
    '_mouse_smoothness': data.mouseBehavior.smoothnessScore,
    // ... остальные метрики
  };
  
  Object.entries(fields).forEach(([name, value]) => {
    const input = document.createElement('input');
    input.type = 'hidden';
    input.name = name;
    input.value = value || 0;
    form.appendChild(input);
  });
}
```

### 3. Настроить серверную проверку
Добавить в backend обработку полей `_mouse_*` и фильтрацию подозрительных заявок.

### 4. Добавить поддержку touch-событий
Для корректной работы на мобильных устройствах.

## 📝 Пример вывода

### Desktop устройство (человек)
```javascript
📊 Полные данные о поведении пользователя: {
  mouseBehavior: {
    directionChanges: 45,
    smoothnessScore: 2.34,
    speedAndAcceleration: 156.78,
    timeBetweenMouseEvents: 12.45,
    mouseActivityBeforeSending: 238
  },
  formFillingTime: 15234,
  device: {
    type: 'desktop',
    os: 'macOS 14.0',
    browser: { name: 'Chrome', version: '120.0.0' },
    touchSupport: false,
    screen: {
      screenWidth: 1920,
      screenHeight: 1080,
      windowWidth: 1440,
      windowHeight: 900,
      pixelRatio: 2
    },
    language: {
      language: 'ru-RU',
      timezone: 'Europe/Moscow'
    },
    hardware: {
      cpuCores: 8,
      deviceMemory: 8
    },
    headless: {
      isHeadless: false,
      suspiciousFeatures: []
    }
  },
  interactionCount: 8
}
```
**Интерпретация:** Все метрики в норме → это человек ✅

### Desktop устройство (бот - headless)
```javascript
📊 Полные данные о поведении пользователя: {
  mouseBehavior: {
    directionChanges: 3,        // ⚠️ Слишком мало
    smoothnessScore: 1.05,      // ⚠️ Слишком прямо
    speedAndAcceleration: 12,   // ⚠️ Постоянная скорость
    timeBetweenMouseEvents: 2,  // ⚠️ Идеально равномерно
    mouseActivityBeforeSending: 5  // ⚠️ Мало активности
  },
  formFillingTime: 850,         // ⚠️ Слишком быстро
  device: {
    type: 'desktop',
    os: 'Linux',
    browser: { name: 'Chrome', version: '120.0.0' },
    touchSupport: false,
    headless: {
      isHeadless: true,         // 🚨 ОБНАРУЖЕН HEADLESS!
      suspiciousFeatures: [
        'webdriver_flag',
        'no_plugins',
        'dimension_mismatch'
      ]
    }
  }
}
```
**Интерпретация:** Множество подозрительных признаков → это бот 🤖

### Mobile устройство (человек)
```javascript
📊 Полные данные о поведении пользователя: {
  mouseBehavior: {
    directionChanges: 0,        // ✅ Норма для мобильных
    smoothnessScore: 0,         // ✅ Норма для мобильных
    speedAndAcceleration: 0,    // ✅ Норма для мобильных
    timeBetweenMouseEvents: 0,  // ✅ Норма для мобильных
    mouseActivityBeforeSending: 0  // ✅ Норма для мобильных
  },
  formFillingTime: 18500,       // ✅ Достаточно времени
  device: {
    type: 'mobile',             // ✅ Мобильное устройство
    os: 'iOS 17.1',
    browser: { name: 'Safari', version: '17.0' },
    touchSupport: true,         // ✅ Поддержка touch
    screen: {
      screenWidth: 390,
      screenHeight: 844,
      pixelRatio: 3
    },
    headless: {
      isHeadless: false,        // ✅ Не headless
      suspiciousFeatures: []
    }
  }
}
```
**Интерпретация:** Мобильное устройство, метрики мыши = 0 это нормально → человек ✅

## ⚡ Производительность

- **Память:** Ограничено 1000 точек траектории (автоматическая очистка старых данных)
- **WeakMap:** Автоматическое освобождение памяти при удалении форм из DOM
- **Event delegation:** Один обработчик на весь документ вместо множества на каждый элемент

## 🐛 Отладка

Откройте консоль браузера (F12) для просмотра логов:
- ✅ Инициализация систем
- ⏱️ Запуск таймеров форм
- 📊 Результаты метрик при отправке

## 📄 Лицензия

Внутренний проект компании. Все права защищены.

---

**Дата создания:** Ноябрь 2025  
**Последнее обновление:** Ноябрь 2025  
**Версия:** 1.1 (добавлено определение устройства и headless detection)

