---
import RedirectPage from '@/layouts/RedirectPage.astro';
import { isRouteDisabled, SITE_NOT_FOUND_PATH } from '@/js/utils/siteRoutes';
if (isRouteDisabled('/redirect/')) {
	return Astro.rewrite(SITE_NOT_FOUND_PATH);
}
---
<RedirectPage
    title="Выгодное предложение месяца"
    description="Получите самое выгодное предложение месяца"
    pageText = 'Вы будете перенаправлены<br/>через 3 секунды...',
    goal="telegram_redirect"
    redirectUrl="https://t.me/alexsab_ru"
    saveUrl=""
    messageToTelegram="Метрика не подключена"
>
</RedirectPage>
