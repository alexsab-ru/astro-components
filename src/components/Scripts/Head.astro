<script type="text/javascript" >
(function() {
    var analyticsLoaded = false;

    function loadAnalyticsScripts() {
        if (!analyticsLoaded && location.host == "livanorenburg.ru") {
            analyticsLoaded = true;

            (function(w,d,s,l,i){w[l]=w[l]||[];w[l].push({'gtm.start':new Date().getTime(),event:'gtm.js'});var f=d.getElementsByTagName(s)[0],j=d.createElement(s),dl=l!='dataLayer'?'&l='+l:'';j.async=true;j.src='https://www.googletagmanager.com/gtm.js?id='+i+dl;f.parentNode.insertBefore(j,f);})(window,document,'script','dataLayer','GTM-NMDLDB3F');

            var script = document.createElement('script');
            script.src = "https://www.google.com/recaptcha/api.js?render=6Lepfy4pAAAAAAGHFP655qNe6Bb_BcskklcxajC6";
            script.async = true;
            script.defer = true;
            document.head.appendChild(script);

        }
    }

    function initializeEventListeners() {
        // Отслеживание скроллинга
        window.addEventListener('scroll', loadAnalyticsScripts);

        // Отслеживание движения мыши
        document.addEventListener('mousemove', loadAnalyticsScripts);
    }

    // Ждем загрузки DOM перед инициализацией
    if (document.readyState === 'loading') {
        document.addEventListener('DOMContentLoaded', initializeEventListeners);
    } else {
        initializeEventListeners();
    }
})();
</script>

