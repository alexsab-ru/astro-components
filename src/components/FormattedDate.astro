---
interface Props {
	date: Date;
}

const { date } = Astro.props;
---
<div class="text-xs text-gray-500">от 
<time datetime={date.toISOString()}>
	{
		date.toLocaleDateString('ru-RU', {
			year: 'numeric',
			month: 'long',
			day: 'numeric',
		})
	}
</time>
</div>
