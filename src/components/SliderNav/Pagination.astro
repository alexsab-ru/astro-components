---
interface Props {
	id?: string;
	class?: string;
}
const { id = '', class: classNames = '' } = Astro.props
import './styles.css'
---

<div class={`slider-pagination ${id}-pagination flex justify-center ${classNames}`}></div>